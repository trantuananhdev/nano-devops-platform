# Core web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1

# Database
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0

# Config & validation
pydantic-settings==2.6.1

# HTTP client — LLM calls + Meilisearch + tool mocks
httpx==0.28.1

# Task queue
celery[redis]==5.4.0
redis==5.2.1

# File upload
python-multipart==0.0.18

# Storage & search
minio==7.2.12              # T-13: MinIO PDF upload
chromadb==0.5.23           # T-15: Vector Memory (HTTP client only)
prometheus-client==0.21.1  # Prometheus metrics exposition

# Auth
passlib[bcrypt]==1.7.4     # T-33: bcrypt key hashing; pinned — passlib 2.x breaks bcrypt compat

# T-28: OpenTelemetry — opt-in, disabled by default (TRACING_ENABLED=false)
# Pinned together — these three must match versions
opentelemetry-sdk==1.24.0
opentelemetry-exporter-otlp-proto-grpc==1.24.0
opentelemetry-instrumentation-fastapi==0.45b0
