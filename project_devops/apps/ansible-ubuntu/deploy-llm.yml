---
# =============================================================================
# deploy-llm.yml — Deploy Gemma 2B llama-server on Ubuntu (LLM node ONLY)
# =============================================================================
# T-55: Kết nối tutinhhao → become root khi cần (dir creation, model download).
# Docker Compose chạy với tutinhhao trực tiếp (thuộc docker group).
#
# Key: prod_deployer (ed25519) — tutinhhao@192.168.100.6
#
# Usage (từ Alpine VM ansible-runner):
#   ansible-playbook -i /tmp/inventory.ini deploy-llm.yml
#   ./cli.sh ansible-deploy-llm
# =============================================================================

- name: "Ubuntu LLM Node — Gemma 2B llama-server"
  hosts: acer
  become: true             # tutinhhao → sudo khi cần (dir, model, UFW)
  gather_facts: true

  roles:
    - role: llm_node
      tags: [llm, gemma]
